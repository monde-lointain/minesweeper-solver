/* overlay.cc — ImGui analysis overlay. STUB (Stream A.0); Stream E implements. */
#include "solver/overlay.h"

void overlay_draw(const struct Analysis *a, const struct Board *b,
                  const struct Layout *lay) {
  (void)a;
  (void)b;
  (void)lay;
}
